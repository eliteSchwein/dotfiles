-- Converted from input.conf to Hyprland Lua config style.

hl.config({
    input = {
        kb_layout = "eu",
        kb_variant = "",
        kb_model = "",
        kb_options = "fkeys:basic_13-24",
        kb_rules = "",

        follow_mouse = 1,

        sensitivity = 0,

        touchpad = {
            natural_scroll = true,
        },
    },
})
