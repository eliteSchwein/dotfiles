-- Converted from decoration.conf to Hyprland Lua config style.

hl.config({
    decoration = {
        rounding = 8,
        rounding_power = 2,

        active_opacity = 1.0,
        inactive_opacity = 1.0,

        shadow = {
            enabled = true,
            range = 4,
            render_power = 2,
            color = "rgba(1a1a1aee)",
        },

        blur = {
            enabled = true,
            size = 1,
            passes = 4,
            xray = false,

            vibrancy = 0.1696,
        },
    },
})
